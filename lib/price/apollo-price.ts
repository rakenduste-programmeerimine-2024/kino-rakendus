import { Show } from "../movie-data/cinemas/apollo-types";



export default function apolloPriceCalculation(show: Show):number{
    //const supabase = createClient();
    let price: number = 0;
    return price
}